{
  "name": "askhuman-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:schema:remote": "wrangler d1 execute askhuman --remote --file=schema.sql -y",
    "db:schema:local": "wrangler d1 execute askhuman --local --file=schema.sql -y"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260722.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.20.0"
  }
}
